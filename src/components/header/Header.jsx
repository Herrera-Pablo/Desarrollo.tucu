// Header.jsx
import React from 'react';
import NavBar from '../navbar/NavBar';

const Header = () => {
    return (
        <header>
            <NavBar />
        </header>
    );
};

export default Header;
